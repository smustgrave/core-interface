{#
/**
 * @file
 * UI Pattern library standalone page, override this in your theme.
 */
#}

{% if pattern is not empty %}
  <div class="pattern-preview pattern-preview__{{ pattern.definition.id }} margin-bottom-6">
    {{ pattern.meta }}

    {# Rendered pattern preview. #}
    {{ pattern.rendered }}
  </div>
{% endif %}
