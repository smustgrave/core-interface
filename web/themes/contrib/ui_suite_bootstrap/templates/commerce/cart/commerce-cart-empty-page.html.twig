{#
/**
 * @file
 *
 * Empty cart page template.
 *
 * @ingroup themeable
 */
#}
{{ include('ui_suite_bootstrap:alert', {
  'message': 'Your shopping cart is empty.'|t,
  'variant': 'info',
}, with_context = false) }}
